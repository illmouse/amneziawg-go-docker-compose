# AmneziaWG Docker Compose VPN

Этот репозиторий содержит **Docker-набор для AmneziaWG** — VPN, похожий на WireGuard, используя контейнер [amneziavpn/amneziawg-go](https://hub.docker.com/r/amneziavpn/amneziawg-go/tags).

Конфигурации сервера и клиента создаются динамически, ключи управляются автоматически через JSON базу данных, что делает систему удобной и повторно используемой.

> [!NOTE]
> Данная конфигурация рассчитана на использование официального контейнера, поэтому все настройки его работы выполняются без пересборки самого контейнера.

---

## 🎯 Особенности

- **Автоматическая генерация ключей** сервера и клиентов при первом запуске
- **JSON база данных** для хранения всех конфигураций и ключей
- **Динамическое управление пирами** - можно добавлять/удалять пиры без пересоздания контейнера
- **Модульная архитектура** - каждый этап настройки в отдельном скрипте
- **Цветные логи с эмодзи** для удобного мониторинга
- **Автоматическое исправление прав доступа** для файлов конфигурации
- **Клиентские конфигурации** генерируются автоматически в `./config/peers/`
- **Логи выводятся** в `docker logs` а также в `./logs/amneziawg.log`

---

## ⚙️ Переменные окружения (`.env`)

```bash
# .env

# Публичный адрес хоста, на котором будут приниматься подключения
WG_ENDPOINT=

# Основные параметры WireGuard
WG_IFACE=wg0
WG_ADDRESS=10.100.0.1/24
WG_PORT=13440
WG_PEER_COUNT=1

# Параметры обфускации AmneziaWG
Jc=3
Jmin=1
Jmax=50
S1=25
S2=72
H1=1411927821
H2=1212681123
H3=1327217326
H4=1515483925
```

**Примечания:**
* Параметры `Jc`, `Jmin`, `Jmax`, `S1`, `S2`, `H1-H4` важны для работы VPN. Подробнее в [документации](https://docs.amnezia.org/documentation/amnezia-wg/#%D0%BF%D0%B0%D1%80%D0%B0%D0%BC%D0%B5%D1%82%D1%80%D1%8B-%D0%BA%D0%BE%D0%BD%D1%84%D0%B8%D0%B3%D1%83%D1%80%D0%B0%D1%86%D0%B8%D0%B8)

---

## 🚀 Как это работает при запуске контейнера

### Процесс запуска контейнера:

1. **🔵 Инициализация окружения** (`init.sh`)
   - Создание необходимых директорий
   - Установка переменных окружения
   - Проверка зависимостей (jq, awg)
   - Создание структуры каталогов

2. **🗃️ Инициализация базы данных** (`config-db.sh`)
   - Создание JSON базы данных `config.json`
   - Запись начальной конфигурации сервера
   - Обновление параметров из переменных окружения

3. **🔑 Генерация ключей сервера** (`server-keys.sh`)
   - Проверка существующих ключей в базе данных
   - Генерация новых ключей при необходимости
   - Сохранение ключей в базу данных

4. **👤 Управление пирами** (`peers.sh`)
   - Сравнение текущего и желаемого количества пиров
   - Генерация новых пиров при необходимости
   - Создание ключей и IP-адресов для каждого пира

5. **⚙️ Генерация конфигураций** (`generate-configs.sh`)
   - Создание конфигурации сервера (`wg0.conf`)
   - Генерация отдельных конфигураций для каждого пира
   - Сравнение и обновление только измененных конфигов

6. **🔒 Исправление прав доступа** (`functions.sh`)
   - Установка прав `700` для директорий
   - Установка прав `600` для всех конфигурационных файлов и ключей
   - Логирование изменений прав доступа

7. **🌐 Запуск WireGuard** (`start-wireguard.sh`)
   - Запуск интерфейса AmneziaWG
   - Применение конфигурации
   - Настройка iptables и NAT
   - Проверка работоспособности

---

## 📊 Управление пирами

1. **При уменьшении `WG_PEER_COUNT`:**
   - Пиры **удаляются из активной конфигурации** (`wg0.conf`)
   - Но **сохраняются конфигурации** для возможного будущего использования в `./config/peers/peerN.conf.removed.20251114`
   - Сохраняются только первые N пиров в алфавитно-числовом порядке (peer1, peer2, peer3...)

2. **При увеличении `WG_PEER_COUNT`:**
   - Система сначала **заполняет пропуски** в нумерации пиров
   - Если есть peer1 и peer3, но нет peer2 - будет создан peer2
   - Затем добавляются новые последовательные пиры

3. **Примеры работы:**

**Сценарий 1: Уменьшение количества пиров**
```bash
# Было: WG_PEER_COUNT=3 (peer1, peer2, peer3 в конфигурации)
# Стало: WG_PEER_COUNT=1
# Результат: В wg0.conf останется только peer1, peer2 и peer3 останутся в БД
```

**Сценарий 2: Увеличение с пропусками**
```bash
# В БД: peer1, peer3 (peer2 удален вручную)
# WG_PEER_COUNT=3
# Результат: Будет создан peer2, затем peer4 НЕ будет создан т.к. нужны 3 пира
```

**Сценарий 3: Последовательное добавление**
```bash
# В БД: peer1, peer2
# WG_PEER_COUNT=4
# Результат: Будут созданы peer3, peer4
```

### Ручное управление пирами:

**Полное удаление пира из системы:**
```bash
# Удалить пира из БД и файловой системы

# удалите пира из config/config.json
nano config/config.json

# Перезапустить для применения изменений
docker compose restart amneziawg
```

**Восстановление пира:**
```bash
# Просто увеличьте WG_PEER_COUNT - система заполнит пропуски автоматически
WG_PEER_COUNT=3
docker compose restart amneziawg
```

---

## 🗃️ Структура базы данных

```json
{
  "server": {
    "interface": "wg0",
    "address": "10.100.0.1/24",
    "port": 13440,
    "endpoint": "your-server-ip",
    "junk": {
      "jc": 3,
      "jmin": 1,
      "jmax": 50,
      "s1": 25,
      "s2": 72,
      "h1": 1411927821,
      "h2": 1212681123,
      "h3": 1327217326,
      "h4": 1515483925
    },
    "keys": {
      "private_key": "server_private_key_here",
      "public_key": "server_public_key_here"
    }
  },
  "peers": {
    "peer1": {
      "name": "peer1",
      "ip": "10.100.0.2/24",
      "private_key": "peer_private_key",
      "public_key": "peer_public_key",
      "preshared_key": "preshared_key",
      "created": "2024-01-15T10:30:00Z"
    },
    "peer2": {
      "...": "..."
    }
  },
  "meta": {
    "version": "1.0",
    "last_updated": "2024-01-15T10:30:00Z"
  }
}
```

---

## 🐳 Пример Docker Compose

```yaml
services:
  amneziawg:
    image: amneziavpn/amneziawg-go:0.2.15
    container_name: amneziawg
    env_file:
      - .env
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      - ${WG_PORT}:${WG_PORT}/udp
    volumes:
      - ./config:/etc/amneziawg
      - ./logs:/var/log/amneziawg
      - ./entrypoint:/entrypoint:ro
    entrypoint: ["/entrypoint/main.sh"]
    healthcheck:
      test: ["CMD", "sh", "-c", "ip link show wg0 && awg show wg0 2>/dev/null | grep -q listening"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: always
    networks:
      awg:

networks:
   awg:
```

**Примечания:**
* `./config` - хранит все ключи, конфигурации и JSON базу данных
* `./logs` - директория для логов приложения
* `./entrypoint` - модульные скрипты инициализации
* `./scripts` - скрипты инициализации для работы `setup.sh`

---

## 📋 Использование

### Быстрый старт:

```bash
# Сделайте скрипты исполняемыми
chmod +x setup.sh
chmod +x entrypoint/*.sh

# Запустите setup скрипт
./setup.sh

# Или запустите вручную
docker compose up -d
```

### Получение клиентских конфигураций:

Конфигурации пиров находятся в:
```
./config/peers/
├── peer1.conf
├── peer2.conf
└── peer3.conf
```

Каждый файл содержит полную конфигурацию для подключения клиента.

### Просмотр логов:

```bash
# Цветные логи с эмодзи
docker logs amneziawg

# Или сырые логи
tail -f ./logs/amneziawg.log
```

---

## 🛠️ Важные замечания

### Безопасность:
- Все ключи и конфигурационные файлы автоматически получают права `600`
- JSON база данных защищена от несанкционированного доступа
- При каждом запуске проверяются и исправляются права доступа

### Миграция и бэкапы:
- Для полной регенерации VPN удалите директорию `./config`
- Для сохранения конфигурации скопируйте `./config/config.json`
- Для миграции на другой сервер перенесите всю директорию `./config`

### Сетевые настройки:
- Убедитесь, что UDP-порт (`WG_PORT`) открыт на роутере/фаерволе
- Проверьте, что `WG_ENDPOINT` содержит правильный публичный IP или доменное имя
- Для работы NAT убедитесь, что IP forwarding включен на хосте

### Устранение неполадок:
```bash
# Проверить статус интерфейса
docker exec amneziawg awg show wg0

# Посмотреть логи инициализации
docker logs amneziawg

# Проверить базу данных
docker exec amneziawg cat /etc/amneziawg/config.json | jq .
```

---

## 🔄 Процесс обновления

При изменении любых параметров в `.env` файле:

1. **Параметры сервера** (порт, endpoint, etc.) - автоматически обновляются в базе данных
2. **Параметры обфускации** - применяются ко всем пирам при регенерации конфигов
3. **Количество пиров** - динамически управляется без потери существующих конфигураций